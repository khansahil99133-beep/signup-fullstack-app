{
  "routes": [
    { "handle": "filesystem" },
    { "src": "/uploads/(.*)", "dest": "/api/uploads/$1" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
